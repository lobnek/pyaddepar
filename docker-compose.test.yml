version: '3.4'
services:
  sut:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    volumes:
      # change the folder on the left to direct your logs to...
      - ./pyaddepar:${WORK}/pyaddepar:ro
      - ./test:${WORK}/test:ro
      - ./source:/source:ro
      - ./artifacts:/pyaddepar/artifacts
    env_file:
      - test/.env-test