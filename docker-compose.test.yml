version: '3.4'
services:
  sut:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    volumes:
      - ./test:/addepar/test:ro
      - ./source:/source:ro
      - ./artifacts:/artifacts
    env_file:
      - test/.env-test
    user: root
